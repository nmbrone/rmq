defmodule RMQ do
  @moduledoc """
  A set of tools for convenient work with RabbitMQ.

  Based on `AMQP` elixir client.

  It includes:

    * `RMQ.Connection`
    * `RMQ.Consumer`
    * `RMQ.RPC`
  """
end
